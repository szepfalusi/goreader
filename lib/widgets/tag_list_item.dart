import 'package:flutter/material.dart';

class TagListItem extends StatelessWidget {
  const TagListItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
